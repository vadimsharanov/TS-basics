enum direction {
  up = "up",
  down = "down",
  left = "left",
  right = "right",
}

enum Sprendimas {
  yes = 1,
  no = calcEnum(),
}

function calcEnum() {
  return 4;
  2343;
}

function runEnum(obj: { up: string }) {}

runEnum(direction);

enum Test {
  A,
}

let test = Test.A;

let nameA = Test[test];

const enum something {
  A,
  B,
}

// let c = something.A;
